[phases.setup]
nixPkgs = ['python311']

[phases.install]
cmds = ['pip install -r requirements.txt']

[phases.build]
cmds = ['echo "Build complete"']

[start]
cmd = 'gunicorn --bind 0.0.0.0:$PORT app:app --timeout 300 --workers 1 --max-requests 50 --preload'